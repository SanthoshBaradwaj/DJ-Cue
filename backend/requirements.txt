# DJ-Cue backend. Supabase is the system of record -- there is no local
# fallback store, so SUPABASE_URL / SUPABASE_ANON_KEY must be set.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
supabase>=2.6

# Dev
pytest>=7.0
